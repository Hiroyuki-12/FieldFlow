{
  "name": "fieldflow-cloudflare",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24.15.0 <25"
  },
  "packageManager": "npm@11.16.0",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "check:deploy": "wrangler deploy --dry-run --outdir .wrangler-dist",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260822.1",
    "typescript": "6.0.3",
    "vitest": "4.1.10",
    "wrangler": "4.125.0"
  }
}
